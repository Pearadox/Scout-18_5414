<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pearadox.scout_5414">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/p_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScoutMaster_Activity" />
        <activity
            android:name=".MatchScoutActivity"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:name=".PitScoutActivity"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".Visualizer_Activity" />

        <activity
            android:name=".FinalActivity"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".ArenaLayoutActivity" />
        <activity
            android:name=".TeleopScoutActivity"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".VisPit_Activity"
            android:configChanges="orientation" />
        <activity
            android:name=".VisMatch_Activity"
            android:configChanges="orientation" />

        <activity android:name=".DraftScout_Activity">
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>